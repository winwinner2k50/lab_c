#include <stdio.h>

#define LEN 100

size_t count_words(char arr_words[][LEN], int arr_count_words[], char s[], size_t len_s);

void arr_str_output(char arr_str[][LEN], int arr_count_words[], size_t n);